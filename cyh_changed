- installed GenomeInfoDbData by:

"""
if (!require("BiocManager", quietly = TRUE))
+     install.packages("BiocManager")
>
> BiocManager::install(c("GenomeInfoDbData", "Rsamtools"), INSTALL_opts="--no-multiarch") # add no multiarch just for this macbook
"""

- clusterGeneSets.R, replace "@counts" with "$counts"

- plotProportions.R: line 132
  - n() -> dplyr::n()
  - ungroup() -> dplyr::ungroup()

- plotViolin.R: remove 'tidyseurat::' from "tidyseurat::mutate""
  - "tidyseurat::mutate"" -> "mutate"

- add "library(tidyseurat)" to vignettes. (for using the function "mutate()")
